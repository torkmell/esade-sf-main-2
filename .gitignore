# Python virtual environment — never upload this
venv/

# Jupyter checkpoints
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# PDF company reports — 342MB total, too large for GitHub; store locally or in shared drive
*.pdf
# ...but the financial filtering framework reference docs ARE small and belong in the repo
!docs/financial_filtering_framework/*.pdf

# Course-provided raw datasets — NOT republished in this public repo.
# These are inputs supplied by the course, not the team's own work; obtain
# them from the course data pack to re-run the pipeline from scratch.
data/provided/equityBicsV2.csv
data/provided/esgEnvironmentalSocialConsolidatedV4.csv
data/provided/esgGovernanceConsolidatedV4.csv
data/provided/legalEntityEuTaxonomy.csv

# Downloaded financial data (regenerate via scripts/financial/fetch_price_data_template.py)
data/financial/raw_prices/
data/financial/raw_benchmark/

# Assignment guidance zip — course material, not needed in repo
*.zip

# Mock price data (large file)
data/market/

# Temp Office lock files
~$*.docx
~$*.xlsx
~$*.pdf

# Python cache
__pycache__/
*.pyc
*.pyo

# OS files
.DS_Store
Thumbs.db

# Sensitive / local config
.env
*.local

# Visual Studio / IDE files
.vs/
.claude/
*.slnx

# Merge backup (created by Final_Deliverables/_execute_merge.py) — too large to commit
Final_Deliverables/_merge_backup/

# yfinance fundamentals cache (regenerated by notebook 04b on demand)
data/market/fundamentals_cache/
